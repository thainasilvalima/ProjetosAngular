import { Component } from "@angular/core";

@Component({
    selector: 'app-header', // diz ao angular qual a diretiva html que será usada para instancias o componente, no caso, será <app-header></app-header> 
    templateUrl: './header.component.html' // diz ao angular onde esta o html associado ao componente, outra diretiva possível é o template que traz o conteúdo hml como strig..
})

export class HeaderComponent {


}